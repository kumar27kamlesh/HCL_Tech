server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/merchant_service?useSSL=false&serverTimezone=UTC
    username: root
    password: princeku

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
